import React from 'react';

import { StyleSheet, View } from 'react-native';

const RecorderDisplay = (props) => {
	const { onPress } = props;

	return <View styles={styles.container} />;
};

const styles = StyleSheet.create({
	container: {}
});

export default RecorderDisplay;
